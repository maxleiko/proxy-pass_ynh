# YuNoHost _ proxy-pass
YuNoHost app that creates an ngix proxy_pass for a specific domain.tld

### Goal
The goal of this application is to create an *nginx* `proxy_pass` configuration
for a specific `domain.tld` based on a given configuration.

### TODO
 - [x] proxy_pass uri
 - [ ] configure nginx location directives in YuNoHost WebAdmin
